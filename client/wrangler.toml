[site]
bucket = "./dist"
entry-point = "."

name = "tennis-league"
pages_build_output_dir = "dist"

[build]
command = "npm run build"
output_dir = "dist"

[build.upload]
format = "directory"

# Enable the Pages Functions API to use advanced functionality
[build.environment]
CLOUDFLARE_PAGES = "true"